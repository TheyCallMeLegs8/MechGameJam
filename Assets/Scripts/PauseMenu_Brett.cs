using UnityEngine;

public class PauseMenu_Brett : MonoBehaviour
{
    // People in youtube tutorials will make a variable public
    // this is cringe unless they are purposefully accessing it from another script
    // otherwise use [SerializeField]
    // Jam a man of fortune
    [SerializeField] private GameObject brett;
}
